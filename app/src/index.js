import React from 'react';
import ReactDOM from 'react-dom/client';

const App = () => <h1>¡Hola desde RTB App!</h1>;

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
